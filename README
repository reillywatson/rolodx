Getting started:
1. Install virtualenv (Ubuntu: sudo apt-get install python-virtualenv, otherwise look it up)
2. Create an environment: 'virtualenv --no-site-packages venv'
3. Install dependencies: 'venv/bin/pip install -r REQUIREMENTS'
4. start server: 'vevn/bin/python rolodx/manage.py runserver'

That should be it.
